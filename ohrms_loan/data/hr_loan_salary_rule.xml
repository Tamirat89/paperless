<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Loan Deduction Salary Rule -->
    <record id="hr_salary_rule_loan_deduction" model="hr.salary.rule">
        <field name="name">Loan Deduction</field>
        <field name="sequence">200</field>
        <field name="code">LOAN</field>
        <field name="category_id" ref="hr_payroll_community.DED"/>
        <field name="condition_select">python</field>
        <field name="condition_python">
            <![CDATA[
result = contract.get_loan_amount(payslip.date_from, payslip.date_to) != 0
            ]]>
        </field>
        <field name="amount_select">code</field>
        <field name="amount_python_compute">
            <![CDATA[
result = contract.get_loan_amount(payslip.date_from, payslip.date_to)
            ]]>
        </field>
        <field name="appears_on_payslip" eval="True"/>
    </record>
</odoo>
